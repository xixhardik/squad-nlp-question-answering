# FastAPI inference backend.
#
# Runtime versions are shared with the ML environment through constraints.txt.
# This keeps serving and training tokenization/model behaviour aligned.

-c ../constraints.txt

fastapi
uvicorn
pydantic
pydantic-settings
torch
transformers
tokenizers
sentencepiece
protobuf
